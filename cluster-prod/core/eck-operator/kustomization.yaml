apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: eck-operator

resources:
  - namespace.yaml

helmCharts:
  - name: eck-operator
    repo: https://helm.elastic.co
    version: 3.4.0   # Sử dụng phiên bản mới nhất
    releaseName: elastic-operator
    namespace: eck-operator
    valuesFile: values.yaml