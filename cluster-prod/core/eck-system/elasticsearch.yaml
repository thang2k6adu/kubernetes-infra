apiVersion: elasticsearch.k8s.elastic.co/v1
kind: Elasticsearch
metadata:
  name: elasticsearch-cluster
  namespace: eck-system
spec:
  version: 9.4.1
  nodeSets:
    - name: default
      count: 1
      config:
        node.store.allow_mmap: false
      # Tùy chọn: giới hạn tài nguyên
      podTemplate:
        spec:
          containers:
            - name: elasticsearch
              resources:
                requests:
                  cpu: 300m
                  memory: 2Gi
                limits:
                  cpu: 1
                  memory: 2Gi
              env:
                - name: ES_JAVA_OPTS
                  value: "-Xms1g -Xmx1g"
      volumeClaimTemplates:
        - metadata:
            name: elasticsearch-data
          spec:
            accessModes: ["ReadWriteOnce"]
            resources:
              requests:
                storage: 20Gi