fullnameOverride: reloader
# watchGlobally mặc định đã true — cần thiết vì service ConfigMap/Secret nằm rải theo namespace
# từng tenant (katech-payment, katech-subscriptions, ...), không phải 1 namespace cố định.
